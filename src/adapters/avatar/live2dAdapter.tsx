'use client';
import type { AvatarProps } from './interface';

export default function Live2DAvatar(_props: AvatarProps) {
  // Placeholder for Live2D SDK integration.
  // When implementing: load the Live2D model and render to a canvas here.
  return null;
}
